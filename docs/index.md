# Documentación de csv2json

La documentación automática se genera con `pdoc` a partir de los docstrings del paquete en `src/csv2json`.

Comando:

```bash
pdoc --output-directory docs/site src/csv2json
```
